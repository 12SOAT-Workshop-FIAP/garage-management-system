apiVersion: batch/v1
kind: Job
metadata:
  name: seed-runner-job
  namespace: fiap-garage
spec:
  template:
    spec:
      containers:
        - name: seed-runner
          image: fiap-garage-api:latest
          command: ['npm', 'run', 'seed:run:js']
          envFrom:
            - configMapRef:
                name: fiap-garage-config
            - secretRef:
                name: fiap-garage-secret
      restartPolicy: Never
  backoffLimit: 2
